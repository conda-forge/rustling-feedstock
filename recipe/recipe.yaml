schema_version: 1

context:
  version: "0.7.0"

package:
  name: rustling
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/r/rustling/rustling-${{ version }}.tar.gz
  sha256: bf67d9a286750e37eff4211e5a3f56133a00b956ed46515f6dd631fc51e70c19

build:
  number: 0
  script:
    - cargo-bundle-licenses --format yaml --output ./THIRDPARTY.yml
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
    - ${{ stdlib('c') }}
  host:
    - python
    - pip
    - maturin >=1.0,<2
  run:
    - python

tests:
  - python:
      imports:
        - rustling

about:
  homepage: https://github.com/jacksonllee/rustling
  license: MIT
  license_file:
    - LICENSE.md
    - THIRDPARTY.yml
  summary: A blazingly fast library for computational linguistics
  documentation: https://docs.rustling.io/

extra:
  recipe-maintainers:
    - jacksonllee
